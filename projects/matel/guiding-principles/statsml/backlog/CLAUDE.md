# Backlog

Unresolved questions, future directions, and topics waiting to be expanded into full docs.

## Simulation Models — topics that could not be generated

These cards were removed from the `85-simulation-models` grid because their detail pages could not be generated (2026-08-28). The grid was then renumbered to stay contiguous: PDE Solvers is card 10 (its pages were later generated and re-included) and N-body & Molecular Dynamics moved from index 13 to 11 (`simulation-models/11-nbody-molecular-dynamics.md/.html`).

- **Cellular Automata** — simple local rules on a lattice (Game of Life, traffic jams, emergent patterns)
- **MCMC & Particle Filters** — working backwards from data to parameters, folding in streaming observations (orphaned files `simulation-models/12-mcmc-particle-filters.md/.html` exist on disk but are unlinked from the grid)
