# k-Means Clustering

**Page type:** detail page (tutorial card-sections: one h2 per section, two-column `table.layout` with text left 50% / canvas right 50%)
**HTML title tag:** k-Means Clustering

**Subtitle:** You say "give me 3 groups" — the algorithm finds where 3 group centers sit and hands every customer to its nearest center

## 200 Customers, No Labels, Three Piles

Tags: `core idea` (blue), `running example` (green)

- **The data** — 200 customers, each just two numbers: monthly spend and visits a month
- **No answer column** — nobody labeled the segments; we want the groups to emerge
- **You choose k** — we ask for k = 3 groups; the algorithm decides who goes where
- **A center per group** — each group is summarized by one point, the average member
- **Membership rule** — every customer simply belongs to whichever center is nearest

*Example (italic):* Like sorting a drawer of receipts into 3 piles when nobody told you what the piles are.

**Key point:** **k-Means:** pick k, then find k center points so that everyone sits close to their nearest center. The "mean" in the name is the group average that each center becomes.

### Visualization (canvas `c1`, 720×300)

Scatter plot of 200 customers colored by segment with ✕ center marks.

- **Title (bold 15px, `#1a5276`, top center):** "200 Customers, k = 3: Each Joins Its Nearest Center (✕)"; muted 12px "illustrative data" caption at top right.
- **Data:** three deterministically jittered clouds generated by a `segment(n, cx, cy, sx, sy, seed)` helper using `Math.sin`-based jitter (no `Math.random()`): SEG1 = 85 points around ($25, 9) with spreads (14, 2.4), seed 1; SEG2 = 70 points around ($75, 5) with spreads (16, 1.9), seed 2; SEG3 = 45 points around ($140, 2) with spreads (15, 1.3), seed 3. Jitter formula per point i: `cx + sx*sin(i*7.13+seed)*(0.4+0.6*|sin(i*2.9+seed)|)`, `cy + sy*sin(i*3.71+seed*2)*(0.4+0.6*|sin(i*5.3+seed)|)`.
- **Centers:** `[[25,9],[75,5],[140,2]]` drawn as dark `#2c3e50` ✕ marks (3.5px stroke, 8px arms).
- **Colors:** SEG1 aqua `#199e70`, SEG2 violet `#4a3aa7`, SEG3 yellow `#c98500`; 4px dots at 0.55 alpha.
- **Axes:** x range 0–165 (labels "$0"…"$160" every $40), y range 0–13 (labels every 4); padding top 42 / bottom 44 / left 52 / right 20; axis titles "monthly spend" (bottom center) and rotated "visits / month" (left); gray `#999` L axes, muted 12px labels.
- **Cluster labels (bold 12px, colored):** "bargain regulars (85)" in aqua near (25, 12.6); "steady mids (70)" in violet near (75, 8.3); "big-ticket occasionals (45)" in yellow near (133, 4.6).
- **Annotation (bold magenta `#d55181` 13px, left-aligned near (78, 11.6)):** "you chose the 3; k-means chose where".

## One Full Round by Hand, Six Customers

Tags: `worked example` (green), `arithmetic` (blue)

- **Six customers** — A(1,1), B(2,1), C(1,2) and D(8,8), E(9,8), F(8,9); ask for k = 2
- **Start** — drop the two centers on customers B(2,1) and E(9,8); any start works here
- **Assign** — A and C are nearest to B's center; D and F are nearest to E's center
- **Move** — each center jumps to its members' average: (1.33, 1.33) and (8.33, 8.33)
- **Repeat** — reassign with the moved centers: nobody switches sides, so it stops
- **Converged** — two rounds and done; that little loop is the whole algorithm

*Example (italic):* New center x = (1 + 2 + 1) ÷ 3 = 1.33 — just the average of A, B, C.

**Key point:** **The loop:** assign everyone to the nearest center, move each center to the average of its members, repeat until nothing moves. Nothing else is happening at any scale.

### Visualization (canvas `c2`, 720×300)

Six-point scatter on a 0–10 × 0–10 grid with center marks walking to the group averages.

- **Title (bold 15px, `#1a5276`, top center):** "Assign → Move: the Centers Walk to the Group Averages".
- **Points:** group 1 aqua `#199e70` 7px dots A(1,1), B(2,1), C(1,2); group 2 violet `#4a3aa7` 7px dots D(8,8), E(9,8), F(8,9); bold dark letter labels above each point.
- **Axes:** x and y both 0–10 with tick labels every 2; padding top 46 / bottom 44 / left 52 / right 200; gray `#999` L axes.
- **Center marks (orange `#d95926` ✕, 3.5px stroke):** faint (0.35 alpha) at the starts B(2,1) and E(9,8); solid at the moved positions (1.33, 1.33) and (8.33, 8.33); orange arrows (2.5px with filled heads) from start to moved position.
- **Move labels (bold orange 12px):** "center moves to (1.33, 1.33)" and "center moves to (8.33, 8.33)" next to the arrows.
- **Legend / story (right column):** aqua dot "joins center 1"; violet dot "joins center 2"; bold orange "✕ faint = start (B, E)" and "✕ solid = after move"; bold green `#008300` 13px two lines "round 2: nobody" / "switches → stop".

## What the Three Segments Buy You

Tags: `where it's used` (blue), `payoff` (green)

- **200 rows become 3 stories** — each segment is described by its center alone
- **Bargain regulars (85)** — around $25 and 9 visits: small basket, constant presence
- **Steady mids (70)** — around $75 and 5 visits: the quiet core of the business
- **Big-ticket occasionals (45)** — around $140 and 2 visits: rare but heavy buyers
- **Same trick elsewhere** — compressing image colors, grouping error logs, user personas

*Example (italic):* Marketing cannot write 200 different emails, but it can happily write 3.

**Key point:** **The algorithm never names the groups:** it returns coordinates, not meanings. "Bargain regulars" is your reading of center ($25, 9) — that interpretation step is on you.

### Visualization (canvas `c3`, 720×300)

Segment profile table drawn on canvas: one row per segment with head-count text plus spend and visits bars.

- **Title (bold 15px, `#1a5276`, top center):** "Each Segment Is Just Its Center: Spend, Visits, Head-Count".
- **Column headers (bold muted 12px):** "segment (customers)" at x=30, "avg spend (scale $0–160)" at x=210, "avg visits (scale 0–10)" at x=490.
- **Rows (name, n, avg spend, avg visits, color):**
  - "bargain regulars" — 85 customers, $25, 9/mo, aqua `#199e70`
  - "steady mids" — 70 customers, $75, 5/mo, violet `#4a3aa7`
  - "big-ticket occasionals" — 45 customers, $140, 2/mo, yellow `#c98500`
- **Layout:** rows start y=66, 62px apart; bold colored segment name with "N customers" beneath; spend bars scaled to $0–160 over 220px starting x=210; visits bars scaled 0–10 over 150px starting x=490; bars 22px tall at 0.75 alpha with bold value labels ("$25", "9/mo", …) just past the bar ends; light `#e5e9ef` vertical rules at the two bar-column origins.
- **Annotation (bold magenta `#d55181` 13px, bottom center):** "3 rows now describe 200 customers — the names are yours, not the algorithm's".

## You Chose k — the Algorithm Never Complains

Tags: `common mistake` (red), `caution` (orange)

- **Ask for 4, get 4** — k-means returns exactly k groups even if the data has 3, or none
- **No error message** — a wrong k does not fail; it just slices real groups arbitrarily
- **Elbow check** — plot total within-group distance for k = 1, 2, 3, ...; look for the bend
- **Here the bend is at 3** — 980 → 410 → 160, then barely improves (135, 118, ...)
- **Random starts matter** — a bad start can wedge; libraries default to k-means++ plus restarts

*Example (italic):* Run k = 3 on customers with no real groups at all and you still get 3 tidy "segments".

**Key point:** **The confusion:** "it returned 3 clusters" is not evidence that 3 clusters exist. The elbow plot — not the output — is where the data gets a say in k.

### Visualization (canvas `c4`, 720×300)

Elbow plot: total within-group distance vs k.

- **Title (bold 15px, `#1a5276`, top center):** "The Elbow: Total Within-Group Distance as k Grows"; muted 12px "illustrative data" caption at top right.
- **Data:** k = `[1, 2, 3, 4, 5, 6, 7, 8]` (x labels "k=1"…"k=8"); inertia = `[980, 410, 160, 135, 118, 104, 95, 88]`; y scale 0–1000 with tick labels every 250.
- **Axes:** padding top 48 / bottom 52 / left 70 / right 30; gray `#999` L axes; x-axis title "number of groups you ask for".
- **Series:** blue `#2a78d6` line, width 3, with 5px blue dots — except the k=3 point which is a larger 7px green `#008300` dot; each point labeled with its value above.
- **Elbow marker:** dashed green vertical line (dash 5/4, width 1.5) from the axis up to the k=3 point.
- **Annotations:** bold green 13px "the bend: big wins stop at k = 3" beside the k=3 point; bold muted `#6b7280` 12px "after 3, extra centers only shave points off real groups" to the right.

## Regeneration instructions

- **Template/layout:** tutorials topic page. h1 (no index number) with 2px `#2980b9` bottom border, `.subtitle` gray one-liner, then 4 `.card-section` blocks. Each section: `<h2>` (1.3rem `#1a5276`, 2px `#2980b9` bottom border) followed by `table.layout` (full width, border-collapse) with one row: `td.text-col` 50% (tags, bullets, `.example`, `.key-point`) and `td.viz-col` 50% (one canvas 720×300).
- **Text column structure:** `.tags` row of pill spans (`.tag.blue` rgba(26,82,118,0.12)/#1a5276, `.tag.green` rgba(39,174,96,0.15)/#27ae60, `.tag.red` rgba(231,76,60,0.12)/#e74c3c, `.tag.orange` rgba(230,126,34,0.15)/#e67e22; 0.72rem, weight 600, radius 10px); `<ul>` of one-line bullets each opening with `<b>` in `#1a5276`; italic `.example` paragraph (`#555`, 0.9rem); `.key-point` callout (background `#f8f9fa`, left border 3px solid `#e74c3c`, padding 8px 12px, 0.9rem) with a `<strong>` lead-in.
- **Page CSS:** body system-ui sans-serif, white background, text `#2c3e50`, padding 40px, line-height 1.6; h1 2rem `#1a5276`; subtitle `#666` 0.95rem; canvases `width:100%` with 1px `#e0e0e0` border, 4px radius.
- **Canvas JS:** shared palette object `P = { blue:#2a78d6, green:#008300, magenta:#d55181, yellow:#c98500, aqua:#199e70, orange:#d95926, violet:#4a3aa7, ink:#1a5276, text:#2c3e50, mute:#6b7280, grid:#e5e9ef }`; shared `setup(id)` helper reading the canvas width/height attributes, scaling the backing store by `window.devicePixelRatio` and calls `ctx.scale` so drawing stays in logical coordinates; point clouds generated with the deterministic sin-jitter `segment()` helper described under c1 (no `Math.random()`).
- **Palette:** primary blue/ink `#1a5276`, green `#27ae60`, red `#e74c3c`, orange `#e67e22`; chart series use the P palette above.
- **Links:** none on this page (no cross-page links, no nav); in regenerated HTML any card links elsewhere use `.html` extensions.
