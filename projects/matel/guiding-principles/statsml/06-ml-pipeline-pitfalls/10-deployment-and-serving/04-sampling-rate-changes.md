# Pitfall: Sampling Rate Changes

**Page type:** detail page (three card-sections, each a two-column layout table: text left 45%, canvas right 55%)
**HTML title tag:** Sampling Rate Changes

**Subtitle:** When data collection frequency changes over time, creating fake trends that look like real behavior shifts.

## The Problem

Tags: `the trap` (red), `sampling` (blue)

- **The trap** — logging or sampling rate changes between periods, shifting event volume
- **Misread signal** — analytics read the volume change as user behavior, not measurement
- **Log sampling** — errors logged at 100% then downsampled to 10% show an illusory drop
- **Instrumentation** — a new SDK logs every click where the old logged every 5th, faking a spike
- **Retention policy** — extending "keep 7 days" to 30 days looks like a real volume increase
- **Noise flips too** — absolute noise shrinks by √10 but relative noise grows by √10

*Example:* A mobile app cuts click logging from 100% to 10% in Jan 2024, and the model learns a fake 90% engagement cliff.

**Impact:** False trends fire drift alarms, models train on inconsistent event densities, and forecasts break at each discontinuity.

### Visualization (canvas `c1`, 720×340)

Time-series line chart showing a fake trend created by a sampling rate change. All series are generated by a seeded Park-Miller LCG (`lcg(20250391)`) — never `Math.random()` — so the plotted points and every printed statistic are identical on every load.

- **Title (bold 14px, `#1a5276`, top center):** "Sampling Rate Change: Fake Trend from Measurement Shift".
- **Axes:** L-shaped axes in `#999` (left x=80, right x=660, bottom y=260, top y=60); x-axis label "Time" centered below, rotated y-axis label "Event Count" on the left, both `#444` 11px.
- **Data generation:** 31 points per period. Pre-change noise is uniform on ±5; post-change noise is uniform on ±5/√10 (≈±1.58), the Poisson-thinning law — 10× fewer events gives √10× smaller absolute noise. Both noise arrays are mean-centered, so the period means land exactly on 150 and 15 and the 10× design is exact.
- **Period 1 (0–60% of x range):** noisy horizontal line in `#1a5276` (width 2.5) at mean 150 events above the axis, 30 segments; bold 11px `#1a5276` label "100% sampling" centered above.
- **Period 2 (60–100% of x range):** noisy horizontal line in `#e74c3c` (width 2.5) at mean 15 events (10× lower); bold 11px `#e74c3c` label "10% sampling" centered above.
- **Change point:** vertical dashed line (`#e67e22`, dash 5/5, width 2) at 60% of x range, with two-line bold 10px `#e67e22` label "Sampling rate" / "changed here".
- **Annotation (left-aligned, right of the change point):** bold 12px `#e74c3c` "Looks like a 90.0% drop" — the percentage is computed at render time as `(1 − mean₂/mean₁)×100` from the plotted values, not hardcoded — then 11px lines "But: True activity unchanged!" and "Only measurement changed."
- **True activity line:** horizontal dashed green line (`#27ae60`, dash 4/4, width 2) across the full width at the computed pre-change mean, labeled "True activity (constant)" in 10px `#27ae60` near the right end.
- **Computed footnotes (two 11px `#444` centered lines below the axis):** "Computed from plotted points: mean 150.0 → 15.0 events = 90.0% apparent drop" and "Noise SD 3.02 → 1.01 events, but relative noise 2.0% → 6.8% — the thinner sample is noisier". Every one of these figures is calculated in JS from the plotted arrays; the values shown here are what seed 20250391 produces.

## Why It Happens

Tags: `root cause` (orange), `sampling` (blue)

- **Infra ownership** — sampling rates live in configs owned by teams optimizing for cost
- **Last to know** — downstream data consumers usually hear about a change after the fact
- **SDK upgrades** — new SDK versions ship with different default logging rates
- **Cost cuts** — downsampling decisions cut storage bills without notifying ML teams
- **A/B instrumentation** — test variants can log at different rates, confounding comparisons
- **Silent changes** — a new data center or sampling config alters volume with no announcement

**Root Cause:** Sampling rate is an infrastructure decision made independently of ML teams, and in raw counts a volume change looks identical to a behavior change.

### Visualization (canvas `c2`, 720×300)

Timeline diagram of event density before and after an SDK upgrade. Dot positions come from a seeded Park-Miller LCG (`lcg(20250392)`) — never `Math.random()` — so the scatter is identical on every load.

- **Title (bold 14px, `#1a5276`, top center):** "SDK Upgrade Changes Logging Rate — Not User Behavior".
- **Timeline:** horizontal line in `#999` (width 2) at y=80 from x=60 to x=660.
- **Divider:** vertical dashed line (`#e67e22`, dash 5/4, width 2) at the timeline midpoint from y=40 to y=180, with bold 11px `#e67e22` label "SDK Upgrade" above it.
- **Dot counts drive the labels:** `n₁ = 60` pre-upgrade, `n₂ = 6` post-upgrade. The logged rate (`n₂/n₁ = 10%`) and the apparent drop (`1 − n₂/n₁ = 90%`) are both computed from these counts at render time.
- **Left half:** bold 11px `#1a5276` label "SDK v1: 100% logging (60 events)" above; 60 dots (radius 2.5, `#1a5276`) scattered in a band below the timeline (y=110 to y=150).
- **Right half:** bold 11px `#e74c3c` label "SDK v2: 10% logging (6 events)" above; 6 dots (radius 2.5, `#e74c3c`) in the same band.
- **Green box:** rectangle at (100, 185) size 520×35, fill `rgba(39,174,96,0.08)`, stroke `#27ae60` width 2, containing centered bold 12px `#27ae60` text "True user behavior: IDENTICAL in both periods".
- **Bottom annotations (centered):** bold 12px `#e74c3c` "Volume drop = measurement artifact, not behavior change"; then 11px `#444` lines "Without metadata, 6 vs 60 logged events looks like a 90% drop in engagement" (counts and percentage interpolated from the actual dot counts) and "Model interprets measurement change as a feature distribution shift".

## The Correct Approach

Tags: `the fix` (green), `normalization` (blue)

- **Carry the denominator** — make the sampling rate part of the data every count travels with
- **Metadata** — store the sampling rate as a column beside event counts in every table
- **Normalization** — divide raw counts by the rate: adjusted_count = raw_count / sampling_rate
- **Segmentation** — if old rates are unrecoverable, train separately on pre- and post-change data
- **Detection** — plot counts annotated with releases; aligned jumps are measurement artifacts

**Fix:** Raw counts are meaningless without the sampling denominator — always normalize as raw_count / sampling_rate.

### Visualization (canvas `c3`, 720×300)

Pipeline flow diagram plus worked normalization examples.

- **Title (bold 14px, `#1a5276`, top center):** "Normalize by Sampling Rate Before Feature Engineering".
- **Pipeline boxes (y=65, height 40):** "Raw Events" (x=50, w=120, stroke `#1a5276`, fill `rgba(26,82,118,0.08)`), "Normalize by / sampling_rate" (two lines, x=240, w=140, stroke `#27ae60`, fill `rgba(39,174,96,0.12)`), "Adjusted / Counts" (two lines, x=450, w=120, stroke `#1a5276`, fill `rgba(26,82,118,0.08)`); bold 11px labels in the box stroke color; gray (`#999`, width 2) connecting arrows with filled triangular heads; a final arrow to the right with bold 11px `#1a5276` text "→ Model".
- **Example section (starting y=130):** bold 12px `#444` heading "Example:"; every number is computed in JS from `rawA = 95, rateA = 0.10` and `rawB = 950, rateB = 1.00`. Row 1: 11px `#444` "Raw count = 95  at  sampling_rate = 10%" with bold 11px `#27ae60` "→  Adjusted = 95 / 0.10 = 950"; row 2: "Raw count = 950  at  sampling_rate = 100%" with "→  Adjusted = 950 / 1.00 = 950"; centered bold 11px `#1a5276` line "Both adjusted counts = 950 — identical signal for the model, though raw counts differ 10x". The two adjusted counts are now exactly equal, so the arithmetic closes.
- **Green success box:** rectangle at (80, 225) size 560×55, fill `rgba(39,174,96,0.08)`, stroke `#27ae60` width 2, with centered bold 12px `#27ae60` "✓  Model sees true activity regardless of measurement changes" and 11px `#444` "Store sampling_rate as metadata; normalize before feature engineering".

## Regeneration instructions

- **Layout:** three `.card-section` blocks ("The Problem", "Why It Happens", "The Correct Approach"), each an h2 with `2px solid #2980b9` bottom border, followed by a full-width `table.layout` with one row: left `td.text-col` (45%) holding tag pills, a bullet list, optional `.example` italic line and a `.key-point` callout; right `td.viz-col` (55%) holding one canvas.
- **Page style:** body system-ui sans-serif, white background, text `#2c3e50`, padding 40px, line-height 1.6; h1 2rem `#1a5276` with `2px solid #2980b9` bottom border; subtitle `#666` 0.95rem; ul 0.92rem with `li b` in `#1a5276`.
- **Tag pills:** `.tag` inline-block, 0.72rem bold, padding 2px 10px, border-radius 10px; blue `rgba(26,82,118,0.12)`/`#1a5276`, green `rgba(39,174,96,0.15)`/`#27ae60`, red `rgba(231,76,60,0.12)`/`#e74c3c`, orange `rgba(230,126,34,0.15)`/`#e67e22`.
- **Key-point callout:** background `#f8f9fa`, left border `3px solid #e74c3c`, padding 8px 12px, 0.9rem, with bold lead-in word. `.example` italic `#555` 0.9rem.
- **Canvas:** `c1` is intrinsic 720×340 (extra height for the two computed footnote lines), `c2`/`c3` are 720×300; CSS `width: 100%`, border `1px solid #e0e0e0`, radius 4px; scaled with `window.devicePixelRatio` via a shared `setup(id)` helper (`canvas.width = 720*dpr`, `ctx.scale` back to logical coordinates).
- **Determinism:** the `<script>` defines a seeded Park-Miller LCG helper (`lcg(seed)`) plus `mean`, `sd`, and `center` helpers immediately after `setup`. `Math.random()` appears nowhere in the page. Chart seeds: `c1` = 20250391, `c2` = 20250392; `c3` generates no random data. Any statistic printed beside generated data is computed from the plotted arrays at render time.
- **Palette:** primary blue `#1a5276`, green `#27ae60`, red `#e74c3c`, orange `#e67e22`, gray axes `#999`, text `#444`/`#666`. No nav bar, no back/home links. In regenerated HTML, any card links use `.html` extensions.
